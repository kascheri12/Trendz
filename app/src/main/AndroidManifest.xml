<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.trendzcatalog.trendz" >

    <uses-sdk tools:overrideLibrary="it.moondroid.coverflow" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/launcher_icon_3"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppThemeTrendz"
        tools:replace="android:icon">
        <activity
            android:name=".activities.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:supportsRtl="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.TranslucentActionBar" >
        </activity>
        <activity
            android:name=".activities.ClosetActivity"
            android:label="ClosetActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.TranslucentActionBar">
        </activity>
        <activity
            android:name=".activities.ScrollingActivity"
            android:label="@string/title_activity_scrolling"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.TranslucentActionBar">
        </activity>
        <activity
            android:name=".activities.LaundryActivity"
            android:label="@string/title_activity_laundry"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.TranslucentActionBar">
        </activity>
        <activity android:name=".activities.FillItUpActivity"
            android:label="FillItUpActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeFullscreen">
        </activity>
    </application>

</manifest>
